Шаг 1 — Установить (всё бесплатно):
  winget install Docker.DockerDesktop
  winget install Python.Python.3.12
  Перезагрузиться.
  
  Шаг 2 — Зарегистрироваться на Groq (бесплатно, без карты):
  → console.groq.com → API Keys → Create → скопировать ключ

  Шаг 3 — Скачать проект и настроить:
  git clone https://github.com/DmitriStarCHE/Pipemail.git
  cd Pipemail
  python -m venv .venv
  .venv\Scripts\pip install -e .
  copy .env.example .env
  В .env поменять:
  LLM_BACKEND=openai
  LLM_MODEL=llama-3.1-8b-instant
  OPENAI_API_KEY=ваш_ключ_от_groq
  OPENAI_BASE_URL=https://api.groq.com/openai/v1
  - заполнить DB_URL, SMTP_* (те же значения что у вас)

  Шаг 4 — Запустить:
  docker compose up -d
  .venv\Scripts\alembic upgrade head
  .venv\Scripts\outreach discover --source 2gis --query "трубы стальные" --region 74 --limit
   10
